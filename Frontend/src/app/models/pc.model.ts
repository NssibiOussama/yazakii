export class pc {
    id !: number
    nom !: string
    prenom !: string
    departement_id !: number
    fonction !: number
    motif !: string
    appareil !: string
    dept !: string
    role !: string
    sig_manager !: number
    sig_hrmanager !: number
    sig_itmanager !: number
    sig_plantmanager !: number
    approuved !: number
}